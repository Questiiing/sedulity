# 「业精于勤」使用说明（v3）

## 这个 App 是做什么的

两个功能，正好一低一高形成"双重关卡"：

1. **保底任务（下限）**：给某个应用（比如微信读书）设"每日最少使用分钟数"和提醒时间。到点还没用够，就发通知提醒你"该去打开它了"。
2. **使用限制（上限）**：给某个应用（比如抖音）设"每日最多使用分钟数"。超限后再打开它，屏幕上会盖一层"今日额度已用完"的拦截层，必须等 30 秒倒计时才能继续——即使你延长了手机自带的屏幕时间限制，这层拦截照样生效。

系统自带的屏幕时间管理只管上限，这个 App 把下限和上限都管起来，适合读书、学英语这类"每天应该完成"的任务，也适合防自己刷短视频。

---

## 一、把 App 装到手机

1. 把「业精于勤.apk」传到红米手机上（微信发给自己、QQ、数据线拷贝，随便哪种都行）。
2. 在手机「文件管理」里找到这个 APK，点击安装。
3. 第一次装非应用商店的应用时，系统会提示"禁止安装未知应用"。按提示去允许：设置 → 应用设置 → 特殊权限 → 安装未知应用，允许「文件管理」（或你用来传文件的 App）安装，然后继续。

> 当前版本 v1.6.8。安装后在主界面左上角「业精于勤」旁边的蓝色版本号上点一下，可以查看版本更新记录。

---

## 二、首次使用：基础授权

打开 App 后，按顺序把下面的权限都开了，功能才完整：

### 1. 使用情况访问（最重要，不开就没法统计）

- 主界面顶部黄色横幅上点「去授权」，会直接跳到设置页；或者手动找：
  **设置 → 应用 → 应用管理 → 业精于勤 → 权限 → 特殊权限 → 使用情况访问 → 允许**
- 找不到就打开手机「设置」，在顶部搜索框输入"使用情况访问"。

### 2. 通知权限

- 首次打开 App 会自动弹窗询问，点「允许」即可。
- 如果之前拒绝了：**设置 → 通知与状态栏 → 通知管理 → 业精于勤 → 允许通知**。

### 3. 精确提醒（建议开启，影响保底任务）

- 主界面如果出现"系统未允许精确提醒"的黄色横幅，点「去开启」。
- 不开启也能用，只是通知可能延迟最多约 15 分钟。

---

## 三、添加保底任务（下限）

1. 主界面默认在「保底任务」页签，右下角点蓝色 **+**。
2. 点「点击选择应用」，搜索并选择要监督的应用（比如微信读书）。
3. 拖动滑杆设置每天目标，比如 30 分钟。
4. 点「提醒时间」设置几点检查，默认 21:00。
5. 建议打开「重复提醒」：到点还没达标的话，每 30 分钟提醒一次，直到当天达标才停。
6. 点右上角「保存」。

---

## 四、添加使用限制（上限）

1. 主界面切到「使用限制」页签，右下角点蓝色 **+**。
2. 选择要限制的应用（比如抖音），拖动滑杆设置每日上限（比如 30 分钟），点「保存」。
3. 超限后再打开这个 App，屏幕会被"今日额度已用完"的拦截层盖住；等 30 秒倒计时才能"继续使用"，点「回桌面」则直接回桌面。继续使用后如果 60 秒内还停留在这个 App，拦截层会再次弹出。

### 使用限制需要先开启「无障碍·实时拦截」（在使用限制页签顶部有横幅引导）

1. **无障碍·实时拦截**：横幅上点「去开启」，在无障碍列表里找到「业精于勤·实时拦截」并开启。它只检测"当前打开了哪个应用"，不收集、不保存、不上传任何屏幕内容。开启后拦截是纯事件驱动的：只有在你真的打开超限应用时才会弹出全屏拦截页，离开这个应用后拦截页自动消失；不会再把拦截盖到别的应用上，待机时也几乎不耗电。
2. **允许受限设置（侧载 App 的坑，非常重要）**：如果无障碍开关能打开、但拦截完全没反应，多半是被小米的「受限设置」拦住了（侧载 APK 在 Android 13+ 常见）。请到：**设置 → 应用 → 应用管理 → 业精于勤 → 右上角「更多」→ 允许受限设置 → 允许**，然后回无障碍里关闭再重新开启「业精于勤·实时拦截」。也推荐用系统自带的「文件管理」安装 APK——用第三方应用市场或浏览器装的侧载包更容易被受限设置卡住。

> 开启无障碍时系统会弹"此服务可以查看你屏幕上的内容"的提示，这是系统对无障碍权限的统一说明；本应用只读取当前应用的包名用于判断是否超限。

> v1.6.0 起拦截页改为全屏页面，已不再需要「显示在其他应用上层」（悬浮窗）权限。

### 遇到问题先看「拦截诊断」和「导出日志」

「使用限制」页签顶部的「拦截诊断」卡片会实时显示：无障碍是否开启、最近事件时间与**投递延迟**、最近识别的前台应用、当前活动窗口。卡片上有三个按钮：

- **运行自检**：一键列出无障碍、事件链路、每个限制应用"系统统计到的时间"，直接告诉你卡在哪一环、下一步该做什么。
- **导出日志**：一键复制完整诊断日志（谁在什么时候收到了什么事件、为什么弹/为什么不弹）。
- **无障碍设置**：直接跳转开启无障碍。

把自检或日志复制出来的内容粘贴发给我，就能精确定位卡在哪一环。

> v1.6.1 修复了"只有回到业精于勤时才弹拦截"的误判：延迟投递的旧事件会被忽略，弹窗前还会二次核对当前活动窗口是否真是那个超限应用。v1.6.2 再加一道保险：只要业精于勤的任何页面正显示在屏幕上，就忽略一切外部应用事件——"回到业精于勤才弹窗"从机制上不可能再发生。v1.6.3 再修两个兼容性隐患：事件时间戳为 0 的系统不再把所有事件误判为过期事件；事件包名缺失时补查次数从 1 次增加到最多 3 次（仍不是轮询），慢切换时检测更稳。v1.6.4 关闭最后两个竞态：业精于勤任何页面一开始创建就立即取消未确认的拦截任务；前台切到其他应用时同样取消——"切走/切回后误弹"在机制上也不存在了。v1.6.5 新增「运行自检」，一键给出每项检查结论。v1.6.6 把"迟到的真事件"也救回来。**v1.6.7 从日志里定位到真正的根因并根治**：HyperOS 会冻结后台进程，导致打开别的应用时无障碍事件要等你回到业精于勤才补送（这就是"打开超限应用没反应、回到业精于勤才有弹窗"的来源）。新增轻量保活服务后，进程不再被冻结，事件实时到达，打开超限应用即可立即拦截。

> 关于保活服务：它是纯"保进程存活"的前台服务，**没有任何轮询和周期任务**，待机几乎不耗电。通知栏会显示一条低优先级的"实时拦截运行中"提示，可以长按该通知设为静默，但请不要在设置里把它关掉（关掉后实时拦截可能失效）。

> 注意：开启无障碍时系统会弹"此服务可以查看你屏幕上的内容"的提示，这是系统对无障碍权限的统一说明；本应用只读取当前应用的包名用于判断是否超限。

---

## 五、小米 / 红米 HyperOS 专属设置（很重要）

小米系统为了省电，会把后台应用清理得很勤，提醒可能因此不准。强烈建议：

1. **省电策略设为无限制**：设置 → 应用 → 应用管理 → 业精于勤 → 省电策略 → 选「无限制」。
2. **允许自启动**：设置 → 应用 → 应用管理 → 业精于勤 → 自启动 → 允许。
3. **允许后台弹出界面**：设置 → 应用 → 应用管理 → 业精于勤 → 权限 → 后台弹出界面 → 允许。不开它，超限后的全屏拦截页可能无法从后台弹出来。
4. 再确认一遍通知权限、无障碍都已打开（尤其装了新版本后，HyperOS 可能把无障碍或自启动偷偷关掉，收不到提醒时先查这里）。

> 拦截页行为说明：打开超限应用时弹出全屏拦截页，30 秒倒计时后才能「继续使用」；点「继续使用」后有 60 秒宽限期，宽限结束再打开超限应用会再次弹出；中途按返回键、回桌面或切换到别的应用，拦截页会自动消失。

---

## 六、常见问题

**Q：到点了没收到保底任务通知？**

先检查：使用情况访问开没开？通知允许没？HyperOS 省电策略是不是"无限制"？都开了还不行，可以重启手机再试（App 会在开机后自动重新安排提醒）。

**Q：提醒过一次后，改了提醒时间就不再通知了？**

这是 v1.6.7 及更早版本的已知问题（改时间后"今日已提醒"状态没重置）。v1.6.8 已修复：保存任务时会重置今日提醒状态，改完时间会按新时间重新提醒。

**Q：超限了但没弹拦截页？**

先打开「使用限制」页签，看顶部的「拦截诊断」卡片：

- 显示"无障碍服务：未开启"→ 去开启无障碍。
- 显示"已开启却收不到事件"→ 多半被「受限设置」拦住了：设置 → 应用 → 应用管理 → 业精于勤 → 右上角更多 → 允许受限设置，然后重新开启无障碍。
- 显示"最近事件"和"最近识别前台"一直在更新、但没弹拦截 → 确认该应用确实设置了上限、且今日已用超过上限；诊断卡上的"最近识别前台"会显示你刚打开的应用名。
- v1.6.7 起请同时确认通知栏有一条"实时拦截运行中"的提示（保活服务在工作）。如果看不到，重新打开一次业精于勤，或重启手机后打开一次 App，保活服务会自动启动。
- v1.6.8 起事件路径不再依赖"当前活动窗口"判断（该 ROM 上它经常错误返回桌面），打开超限应用时该弹就弹；每 15 分钟的兜底检查也会直接弹拦截页而不是只发通知。

另外确认「后台弹出界面」已允许，HyperOS 可能偷偷关掉无障碍或后台弹出界面，重新开启即可。

> 重要：如果你的日志显示"打开别的应用期间事件空白、回到业精于勤时补送一堆延迟事件"，那就是 HyperOS 冻结了进程——请务必让保活服务运行，并把 省电策略 设为「无限制」、允许自启动。

如果卡片信息都对但拦截还是不弹，点「导出日志」→「复制日志」，把内容发给我。日志里会明确写出：事件是否到达、延迟多少毫秒、是否通过二次核对、拦截页是否启动成功。

**Q：拦截层弹到别的应用上了 / 老是重复提醒已超限？**

这是 v1.5.0 起修复、v1.6.8 进一步根治的问题：拦截只在无障碍事件检测到你真的打开超限应用时才触发；延迟投递的旧事件会被忽略，且业精于勤界面显示时直接忽略一切外部事件，所以打开业精于勤本身不会再触发；离开后自动消失，重复提醒每 10 分钟最多一次。请更新到 v1.6.8，并确认无障碍已开启、保活服务在运行。

**Q：怎么看版本和更新记录？**

主界面左上角「业精于勤」旁边的蓝色版本号（v1.6.7），点一下就能看到从 v1.0.0 开始的更新记录。

**Q：显示的使用分钟数偏少？**

系统使用统计本身可能有几分钟延迟；另外只统计"应用在前台"的时间，锁屏播放、后台运行都不算。

**Q：想改或删除任务 / 限制？**

主界面对应页签点卡片进入编辑页，改完点「保存」；底部有删除按钮。

**Q：装不上 / 提示"应用未安装"？**

确认手机是 Android 7.0 及以上；如果装过旧版，直接覆盖安装即可。

---

## 七、以后想自己改代码（可选）

- 源码就在本文件夹，用 Android Studio 打开就能改。
- 本机命令行构建环境在 D 盘（JDK / Android SDK / Gradle 都在 `D:\DevTools`），构建命令：

```
. D:\DevTools\build.ps1
cd C:\Users\Questiiing\Desktop\项目尝试\sedulity
gradle assembleDebug
```

- 打包好的 APK 会输出到 `app\build\outputs\apk\debug\`。
